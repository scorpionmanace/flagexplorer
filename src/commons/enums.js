// ListTypes enums to make sure if the list selection is single selection or multiple entry selection
const ListTypes = {
  multi: "multi",
  single: "single"
};

// Display mode for the stepper can be read only or write type
const StepperType = {
  write: "writeonly",
  read: "readonly"
};

export { ListTypes, StepperType };
